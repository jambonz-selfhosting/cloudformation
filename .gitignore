CLAUDE.md

# Generated CloudFormation templates (user-specific, not committed)
jambonz-mini-*.yaml
jambonz-medium-*.yaml
jambonz-large-*.yaml

# Backup files (numbered backups created by generate-cf.sh)
*.yaml.backup.*

# Base templates use underscore prefix and should be committed
# (no exclusion needed - underscore prefix makes them distinct)
